# 2026-08-31 工作日志

## git 回退：回到 c7a5172（撤销今晚 AI 两波提交）
- 起因：用户判定最近版本被 AI 改坏，要求回退。
- 备份提交 `6340cee`（02:21:30 时间戳）：暂存工作区未提交改动 `portfile.cmake` + 新建 `libredwg_memory_cancel.patch` 到历史。
- 两次 revert（保留历史）：
  - `ea6609f` Revert `2eb9e5c`（libredwg overlay port 固化，8 文件删除）
  - `eabfcf3` Revert `7234c08`（DXF/DWG 改走 HeavyLane 路由，ImageEngine.cpp 回 11 行）
- 结果：工作树 == c7a5172 内容，DXF 预览修复 v6.30.12 保留，v6.30.13 路由撤销，libredwg 回落 vcpkg 官方 port。
- 冲突处理：revert 2eb9e5c 时 portfile.cmake 因备份提交改动产生 modify/delete 冲突，已 `git rm -f` 解决。
- 注意：third_party/vcpkg 子模块内部 libdxfrw 等 7 处改动**不在 revert 范围**，原样保留；若"坏"源自子模块改动，主仓库回退无效。

## 编译启动验收（02:35）
- 后台跑 `看图软件编译并启动.ps1` 卡死在 `-- Running vcpkg install`（代理 `127.0.0.1:7890` 不通，vcpkg 静默阻塞下载 libredwg）。
- 绕开法：不重 configure，直接增量 `cmake --build out/build/Release-LTO`。
- **关键发现：Release-LTO 的 `vcpkg_installed/` 整目录为空**（之前从未成功 install，02:41 那版 exe 非从该配置正常链接）。Release-NoLTO 同 triplet `x64-windows-static-clang` 的 lib 完好。
- 解法：robocopy /MIR 把 NoLTO 的 `vcpkg_installed` 镜像到 Release-LTO（725MB），libredwg.lib 就位；build.ninja 完整、CMakeLists 未动 → ninja 只重编 ImageEngine.cpp 一 TU + LTO 重链，绕开 vcpkg 网络。
- 启动前确认无旧 QuickView 进程（单实例互斥），启动成功 PID=35196，新 exe 路径 = Release-LTO。
- BUILD_EXIT=0，无 360 拦截写 exe。

## 经验
- revert 含"新增目录"的提交时，若该目录有后续未提交改动，必触发 modify/delete 冲突，需先备份提交再 `git rm -f` 解决，不能直接 `--abort`（会丢失工作成果）。
- **Release-LTO 的 vcpkg_installed 可能为空**（vcpkg 代理卡死从未装通）。增量 build 报"找不到 libredwg.lib"时，先镜像 NoLTO 同 triplet 的 vcpkg_installed，别急着重 configure（会再卡代理）。

## git 备份推送 (09:28)
- 推送 `main`(84915e9..eabfcf3, 14 提交含本次回退 3 提交) + tag `backup/2026-08-31-revert-c7a5172` 到 origin，PUSH_EXIT=0。
- 明确指定 ref 未用 `--tags`；远程原本已有几十个历史 v*/旧 backup tag，本次未新增，符合"群里不要旧 tag"约定。
- 10:51 用户复核"git备份推送"：重新 push origin main + 该 backup tag 均为 Everything up-to-date，确认回退代码与备份 tag 均在 origin 就位；当前在 `main` 分支、无独立开发分支、无未合并分支，"分支合并"问题不存在。
