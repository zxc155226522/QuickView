# 2026-08-03

- 继续实施已批准的 MuPDF 1.27.2 PDF/PDF-compatible AI 支持方案。已核验 `QuickView/main.cpp` 的文档控制器初始化、结果消息处理和结果上传代码实际落盘；补充 `StartNavigation()` 的 PDF/AI 专用分派，避免进入普通图片占位和解码管线；为 MuPDF context 注册增加错误边界，并校验 MuPDF 页面输出为四通道 BGR+Alpha 后再复制为 BGRA。当前 `git diff --check` 通过，页内优先导航、交互停止后高清重渲染、完整编译测试与最终 Git 快照仍待完成。
- Release-LTO 已改为严格从 `E:\qv_build_tmp` junction 配置。OpenJPEG 和 libpng 已成功构建；修复了自定义 libpng 同时依赖 `zlib-ng` 与项目 zlib 兼容端口造成的文件冲突，统一为 `zlib`/`zlibstatic.lib`。Windows vcpkg 解压阶段连续出现旧 buildtrees 目录占用，采用改名隔离而非删除；当前已隔离 FreeType 残留并继续配置。另新增 MuPDF/字体第三方许可索引、对应源码说明和完整 AGPLv3 原文，并接入便携发布脚本及安装器。
- FreeType 已进入真实编译，失败点定位为 vcpkg 默认 `CMAKE_RC_FLAGS=/c65001 /DWIN32` 被 CMake 同时传给 `clang-cl` 资源预处理阶段，导致 `/c65001` 被误判为文件。已在 `custom-triplet/x64-windows-static-clang.cmake` 对第三方端口显式覆盖 `CMAKE_RC_FLAGS=/DWIN32`，隔离旧 FreeType 构建目录并重试。
- 用户决定放弃本次 PDF/AI/MuPDF 实施。已停止后台构建，按修改前快照 `e0e654d` 恢复全部已跟踪源码、CMake、测试、脚本和安装器文件；本次新增文件已移出工作树并归档到 `out/rollback-ai-pdf-20260803-0216`。回退后 `git status` 仅剩实施前已有的 `third_party/vcpkg` 子模块状态和 `.workbuddy/` 未跟踪目录，`git diff --check` 通过。
