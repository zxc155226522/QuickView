# 2026-08-02

- 完成“自绘标题栏、恢复右上角窗口按钮、窗口内容内边距、图片居中、鼠标拖动图片而非窗口”的代码现状调研并提交待审批方案。关键现状：`UIRenderer::DrawWindowControls` 已有可复用的右上角关闭/最大化/最小化/置顶按钮，但受 `AutoHideWindowControls` 控制；`WM_LBUTTONDOWN` 默认按 `LeftDragAction=WindowDrag` 移动窗口；图片 PanX/PanY 管线已有，但 `CanPan` 与 `ClampPanForViewport` 会阻止适应窗口图片自由平移。建议以 36 DIP 自绘标题栏、12 DIP 内容内边距为默认实现，标题栏空白区拖窗，图片区左键拖图片。
- 仓库当前存在用户原有未提交状态：`third_party/vcpkg` 子模块工作区为 dirty；后续备份提交必须排除该状态。
- 已完成并验收窗口与图片交互改造：新增 36 DIP 自绘标题栏并常驻显示置顶/最小化/最大化还原/关闭按钮；普通窗口沿用 Windows 11 DWM 圆角；图片使用扣除标题栏、固定画廊和四周 12 DIP 内边距后的有效视口居中；图片区左键固定拖图，窗口仅由标题栏空白区拖动；标题栏双击不最大化，图片区双击与比较模式双击均固定恢复完整居中的适应窗口；SVG 视口避免重复中心偏移。修改文件：`QuickView/EditState.h`、`QuickView/SettingsOverlay.cpp`、`QuickView/UIRenderer.cpp`、`QuickView/UIRenderer.h`、`QuickView/main.cpp`。
- 最终 `Release-LTO` 增量构建成功，完成 7 个编译/链接步骤并生成 `out/build/Release-LTO/QuickView.exe`；启动冒烟检查通过，`git diff --check` 通过。修改前备份提交为 `c77a6768739ccc5d2471499a67ef19f86748cd4d`，完成提交为 `7b6f906feb5f8c8b91d9e7c093496672021c760e`。完成提交仅包含上述 5 个源文件，保留用户原有 `third_party/vcpkg` dirty 状态，`.workbuddy/` 未纳入提交。
- 用户进一步澄清：右上角窗口按钮必须直接嵌入标题栏右侧，不要胶囊底板、阴影和四周留白造成的悬浮效果。已确认当前悬浮感来自 `UIRenderer::DrawWindowControls` 的 `marginX/marginY/padX/padY`、`capsuleRect`、GeekGlass 背景及径向光晕。
- 已完成内嵌标题栏按钮改造：`QuickView/UIRenderer.cpp` 将四个按钮改为宽 `42 DIP`、高与 `36 DIP` 标题栏一致的连续矩形单元，紧贴标题栏右侧；删除胶囊容器、GeekGlass、圆角底板、阴影与径向光晕；普通按钮悬停仅显示主题矩形覆盖层，关闭按钮悬停显示红色矩形；同步更新点击矩形、标题文字避让和标题栏拖动边界。`Release-LTO` 构建、链接、启动冒烟和 `git diff --check` 均通过。修改前备份提交 `3d11805`，完成提交 `4a208ce`；用户原有 `third_party/vcpkg` dirty 状态仍保留。
- 已定位并修复 CDR/CMX 初始显示被过度放大和裁切的根因：`librevenge` 根 SVG 输出物理尺寸（如 `width="8.5in"`）及点坐标 `viewBox`，而 `LoadCDR()` 旧逻辑仅用 `strtof()` 将其误读为 `8.5px`。`QuickView/ImageLoader.cpp` 现复用原生 SVG 的 `GetSvgRootAttrVal`、`TryParseSvgLengthToDip` 和 `TryParseSvgViewBoxSize`，按 96 DPI 正确换算 `px/mm/cm/in/pt/pc/q`，失败时回退 `viewBox`。`QuickView/main.cpp` 同步把缺失 INI 键时的 `LockWindowSize`、`KeepWindowSizeOnNav`、`RememberLastWindowSizeAndPosition` 默认值改为 `true`，与结构体默认值一致；已有明确用户配置仍受尊重。修改前提交 `9b2b368`，完成提交 `bee05a0`。`Release-LTO` 全量构建 `113/113` 成功，EXE 启动并保持响应超过 2 秒；产物大小 `9143296` 字节，SHA-256 `083a2f95aaa3f6c226f88ac5e4744451c6bf50e35d14850a1f4e25713d6327c8`。仓库没有 CDR/CMX 样例，仍需用户用原问题文件做视觉复验。
- 完成“标题栏在上、图片在下且不悬浮”的只读架构分析。确认当前 `m_imageContainer` 只有变换、没有窗口内容区裁剪；标题栏由更高层 `m_staticLayer` 最后绘制，实际仍是全窗口图片层被标题栏覆盖。最小可靠方案需引入共享 `ImageViewportLayout`（明确 `Left/Top/Right/Bottom/Width/Height/CenterOffset`），由 `CompositionEngine` 使用独立未变换裁剪父容器或等价窗口坐标 clip 限制图片；普通位图、SVG、比较模式、缩放动画必须复用同一布局。全屏顶部恢复为 0，固定画廊、DPI 和窗口尺寸变化随 `SyncDCompState` 同步。当前尚未修改代码，待用户审批结构化计划。
- 已完成标题栏与图片固定上下分区改造。新增 `QuickView/ImageViewportLayout.h/.cpp`，统一计算普通窗口的 `36 DIP` 标题栏、固定画廊与 `12 DIP` 图片内边距；全屏时标题栏、画廊占位和内边距全部取消。`CompositionEngine` 新增未变换 `m_imageViewport` 父 Visual 和 `IDCompositionRectangleClip`，将承载缩放/旋转/平移的 `m_imageContainer` 放入其中，图片像素在渲染边界上无法进入标题栏。普通位图、SVG/CDR、Titan、比较模式、平滑缩放、100%、填充、缩放百分比、鼠标锚点、窗口边缘比例吸附和边界指示器均复用共享内容区。修改前提交 `1f1b491`，完成提交 `e0a5f50`；最终 `Release-LTO` 构建成功，启动冒烟结果 `Running=True`、受控关闭 `Closed=True`，EXE 为 `9138688` 字节，SHA-256 `829e769cd6c062768b068051a64162e16e3250433f26171c96100c6970660fb4`。当前 CMake 配置未生成 `QuickViewTests` 目标；用户原有 `third_party/vcpkg` dirty 状态和 `.workbuddy/` 未纳入提交。
- 完成标题栏置顶按钮视觉需求的只读核查：当前 `QuickView/UIRenderer.cpp::DrawWindowControls` 在未置顶时绘制 `Pin`，置顶后切换为带斜线的 `Unpin` 并使用蓝色。用户要求置顶后不显示横线，改为继续绘制同一个 `Pin` 图标、旋转为竖直正向并显示蓝色；预计仅需修改 `QuickView/UIRenderer.cpp` 的局部绘制逻辑，置顶状态与点击行为无需改动，待审批后执行 Git 备份和代码修改。
- 已完成标题栏置顶按钮视觉修改：`QuickView/UIRenderer.cpp::DrawWindowControls` 的局部 `DrawIcon` 支持可选旋转角度；置顶激活态不再使用带斜线的 `Unpin`，而是继续绘制 `Pin`、旋转 `-45°` 成竖直正向并使用既有蓝色强调色。`Release-LTO` 全量构建 `114/114` 成功，程序已启动；EXE 大小 `9,138,688` 字节，SHA-256 `b475b37518cb838f6c27583d275ccaf8abd83c392f2e244f62f500ab44deadbb`。修改前提交 `c84e8dd`，完成提交 `80c3711`。提交时发现 PowerShell 沙箱会生成提交对象但未持久化当前分支引用，已验证提交父子链后用 `git update-ref` 安全恢复；最终仅保留用户原有 `third_party/vcpkg` dirty 和 `.workbuddy/` 未跟踪状态。
- 完成“把图片信息从图片区域移到标题栏”的只读定位：当前精简信息由 `QuickView/UIRenderer.cpp::DrawCompactInfo` 覆盖绘制在图片区域，文本由 `BuildCompactInfoText`/`FormatLiteField` 生成；自绘标题栏由同文件 `DrawTitleBar` 绘制，具备省略与窗口按钮避让。最小方案预计仅修改 `QuickView/UIRenderer.cpp`，复用精简信息构建逻辑到标题栏并停止在图片区域绘制；详细 EXIF 面板是否也取消需用户确认，尚未改代码。
- 已按用户批准完成精简图片信息迁移：`QuickView/UIRenderer.cpp::DrawTitleBar` 在精简信息模式下复用 `BuildCompactInfoText`，动画图片沿用帧序号/延时/处置方式/尺寸/文件名文本；标题栏自动按窗口按钮剩余宽度末尾省略。静态层不再调用 `DrawCompactInfo`，并清空旧面板点击区域；`GetRequiredInfoPanelSize` 对精简模式返回零，不再因标题栏信息额外扩大窗口。详细 EXIF 面板仍保留。修改前提交 `5e6ba70`，完成提交 `e9b7366`；`Release-LTO` 构建 15/15 成功，直接启动后进程持续运行超过 16 秒，EXE SHA-256 为 `fa4248af549bf2078e5ccd5bf555d1eb71052a96b047eaa5948bdf2210df59b4`。仅提交 `QuickView/UIRenderer.cpp`，用户原有 `third_party/vcpkg` dirty 与 `.workbuddy/` 未跟踪状态保留。
- 完成 Adobe Illustrator `.ai`（PDF 兼容型）支持的只读选型调研。项目当前没有 PDF 引擎，`.ai` 未注册；最低系统为 Windows 10 1511，渲染管线已使用 Direct2D/DirectComposition 和 `IDXGISurface`。Adobe 官方说明“Create PDF Compatible File”会在 AI 中保存 PDF 表示；XnView/IrfanView/ImageMagick 通常依赖 Ghostscript，Inkscape 对 AI 9+按 PDF 导入。针对 QuickView，首选 Windows 原生 `Windows.Data.Pdf`/`IPdfRendererNative`：可按源矩形和目标尺寸直接渲染到 DXGI surface，无新增第三方依赖和许可证负担；实现应使用原生 COM/ABI 或隔离适配层，避免与项目 `/EHs-c-` 禁异常设置冲突。若未来要求跨平台或更完整 PDF 功能，备选 PDFium；不建议优先采用 MuPDF/Ghostscript（AGPL/商业双许可）或 Poppler（GPL）。非 PDF-compatible AI 与旧版 PostScript AI 不在首期支持范围。
- 用户倾向在 AI/PDF 支持中采用 MuPDF，并明确要求分页、CMYK 色彩支持和清晰缩放。版本建议固定为官方稳定补丁版 MuPDF Core `1.27.2` 的 C API；不采用 master，也不采用当前官方信息仍带 release-candidate 描述的 1.28 系列。编译配置需保留 PDF、ICC/LCMS2、CMYK plotter、JPEG2000，并启用 spot/overprint simulation；屏幕显示应将文档 CMYK/ICCBased/DeviceN 经 ICC 转换到显示器 RGB，而不是直接显示四通道 CMYK。清晰度依赖按当前 DPI/缩放倍率重新矢量栅格化、DisplayList 缓存和高倍率分块渲染，而非一次低分辨率位图放大。尚未修改代码，许可证仍需用户最终接受 AGPLv3 或商业许可。
- 已进一步明确 MuPDF 接入边界：开源版为 AGPLv3，分发集成后的 QuickView 二进制时，应向接收者提供对应源码，包括实际使用的 MuPDF 源码及修改、QuickView 集成源码、构建与依赖配置、安装运行所需控制脚本和许可证声明；仅本地自用且不分发通常不触发源码交付，纯桌面本地查看也不会额外触发 AGPL 第 13 条网络交互要求。GPLv3 QuickView 与 AGPLv3 MuPDF 可按 AGPLv3 第 13 条组合，但各自覆盖部分继续适用相应许可证，发布流程仍应满足 Artifex 对完整系统源码的要求或购买商业许可。性能方面不能承诺所有文件固定秒开；产品目标应定义为窗口尺寸首屏快速可见，采用低分辨率首屏、Display List 缓存、后台高清重渲染和高倍率可视区分块，普通页面争取约 100–500 ms 出首屏，复杂 CMYK/透明度/专色/叠印页面可能需 1–3 秒或更久，最终需用真实 AI/PDF 样本基准测试。
- 用户已发出“开工”指令。完成实施前复核：当前分支 `feat/ai-pdf-viewer`、HEAD `e9b7366`，产品代码无未提交修改，既有 `third_party/vcpkg` dirty 与 `.workbuddy/` 未跟踪状态需继续排除。确认首期应把 PDF/AI 建模为独立分页矢量文档，不能复用带定时播放语义的 `IAnimationDecoder`；建议新增 MuPDF 文档封装和异步页面渲染控制器，首屏按窗口尺寸渲染，中倍率全页重渲染，高倍率按可视区加预取边缘渲染。依赖通过自定义 vcpkg overlay port 固定 MuPDF 1.27.2；官方 Windows 工程默认 MSBuild 静态库、v142 和动态 CRT，接入时必须适配项目静态 CRT、Clang-cl/链接环境并裁剪 OCR/条码等非查看组件。已形成文件级实施计划，等待用户明确批准后再执行修改前 Git 备份和产品代码变更。
- 完成 CDR 导出的 PLT/DXF 预览接入调研，未修改代码。Corel 官方资料确认 PLT 是 HPGL/HPGL2 子集，单位 1016 plotter units/inch，导出仅保留轮廓，贝塞尔曲线转线段，颜色最多 8 种；DXF 导出通常仅轮廓，文本格式不保留，3D 不支持，版本范围依 Corel 版本可到 2013。推荐开源路线：PLT 用 LGPL-2.1-or-later 的 HPGS 1.1.8（进程内 C API、内存输入、HPGL/2/PCL 图片、可自定义矢量 device 回调，生成 SVG 后复用 Direct2D）；DXF 用 BSD-3-Clause 的 DIME（项目已有 vcpkg overlay port，避免 `libdxfrw` 的 GPL-2.0-only 许可证风险）。`hp2xx` GPL-2-or-later 可作兼容性兜底但属于老式转换器；商业备选 CADSoftTools CAD DLL 可同时读 DXF/HPGL/PLT，但需商业授权。已形成审批前文件级计划，等待用户提供同一 CDR 导出的 PLT/DXF 样本并明确批准后，按规范先做 Git 快照再改代码。
- 核实 CADSoftTools CAD DLL 许可：项目本身开源不产生免费使用权。未注册/试用版仅允许测试，生产用途必须注册购买；标准授权按 Named Developer 收费。协议第 5.1 条禁止分发厂商提供的源代码，第 6.2 条禁止用其创建与 CADSoftTools 产品直接或间接竞争的免费、共享或商业产品；QuickView 作为文件查看器存在明显触发风险。此外 QuickView 为 GPLv3，分发与专有 CAD DLL 链接的组合程序还存在 GPL 兼容性问题。结论是不采用标准 CAD DLL 授权；除非厂商书面提供专门开源/GPL 兼容许可并明确豁免竞争限制。
- 用户倾向 PLT 直接绘制以提高速度。基于所有 PLT 均由 CorelDRAW 导出的受控来源，推荐调整为样本驱动的轻量 HP-GL 解析器：首期仅实现 Corel 实际输出的 IN/SP/PU/PD/PA/PR/AA/AR/CI/LT/PW/PC/IP/SC/IW/PG 等命令，解析一次生成 Direct2D PathGeometry/笔样式场景缓存，缩放和平移直接复用 GPU 矢量场景，不经 SVG；未知命令应安全跳过并记录。HPGS 保留为参考或遇到复杂 HPGL/2/PCL 包装时的后备，不作为首期硬依赖。DXF 仍建议使用 DIME 解析，因为 DXF 实体、块、图层、样条和文字规则复杂，不适合自写解析器。
- 完成桌面 2 个 PLT、2 个 DXF 的只读样本核验并校准方案。两个 PLT 实际命令白名单均仅为 `IN/VS/WU/PW/LT/SP/PU/PD`，可用轻量状态机合并连续折线后原生绘制。老式 DXF 无 HEADER，仅含 103 POLYLINE、5815 VERTEX、48 LINE、21 TEXT，全部位于图层 11，颜色/线宽/线型均 BYLAYER，文件实际可按 GBK/GB18030 解码。新 DXF 为 AC1032/ANSI_936，含 744 个三次 SPLINE（701 个标志 11、28 个标志 24、10 个标志 8、5 个标志 27；均无权重和拟合点，节点/控制点计数全部一致）及 2 个 IMAGE；两个外部 JPEG `_ACD_0.jpg`、`_ACD_1.jpg` 均在桌面实际存在。核实 DIME 0.9 当前源码：原生解析 SPLINE 的 degree/knots/control points/weights，但不提供样条几何提取；IMAGE 作为 UnknownEntity 保留；OBJECTS 在 Section 工厂中未启用专用解析而整段作为 UnknownSection，且原始记录没有公开访问接口，不能直接用高级 API 解析 IMAGEDEF。首期建议 PLT 与 DXF 共用 CPU 侧 VectorDocument 场景，GPU 资源在主线程按 D2D factory 构建；DXF 用 DIME + TinySpline（MIT，需增加 overlay port）将 B-spline 分解为 cubic Bezier，先支持轮廓、颜色和线宽，外部 IMAGE 延后到第二阶段。
