# 2026-08-11 缩略图 ShellEx 收尾与 UserChoice 覆盖层根因

## 事实（注册表实测）
- `HKCU\.svg` 默认 = `QuickView.Vector`；`QuickView.Vector\ShellEx\{E357FCCD...}` = 我们的 CLSID；`.svg\ShellEx`(扩展名级) = 我们的 CLSID —— **我们注册没问题**。
- `FileExts\.svg\UserChoice` = `WPS.PIC.svg`（哈希 `V3GpbncZ/mg=` 未变），受保护。
- `WPS.PIC.svg\ShellEx` 在用户「关 WPS 关联」时被 WPS 重置抹掉（之前注入的被擦）。
- 对照 `.ai\UserChoice` = (not found) → 无覆盖层 → 正常出图。

## 根因
Windows 缩略图归属优先级：`UserChoice`(受保护) > ProgID 级 ShellEx > 扩展名级 ShellEx。
`.svg` 的 UserChoice 锁死 WPS，覆盖了我们所有注册。AI/PDF 能出是因链路最终落到 QuickView.Vector。
用户直觉「你们没注册到」只对一半：注册了，但没抢 UserChoice 这层（RegisterAssociations 不碰 UserChoice）。

## 已验证的死路
- 删 `UserChoice` 键：PowerShell `Remove-Item` 报 "Cannot delete a subkey tree because the subkey does not exist" —— 哈希保护，标准 API 删不掉。
- 清 `thumbcache_*.db`：系统持锁 + explorer 被杀瞬时自启重新加锁，删不掉（但无害，无负向缓存概念）。

## 临时修复（已实施）
向 `WPS.PIC.svg\ShellEx\{E357FCCD...}` 注入我们的 CLSID（WPS.PIC.svg 现为 handler-less 空 ProgID，UserChoice 仍指向它）→ Explorer 经 UserChoice 走到该 ProgID 即命中我们 provider。待用户目视验证。

## 永久修复方向（待用户批准）
1. 路A（用户）：Windows 设置→默认应用→.svg→QuickView，合法重写 UserChoice，且 WPS 已关关联不会抢回。
2. 路B（代码）：RegisterAssociations 中遍历各格式 UserChoice 当前 ProgID，若非我们自己的则向其注入 ShellEx，每次启动关联自动补，抗 WPS 抹除。

## 增补：为什么「手动设置能、裸写注册表不能」（用户 02:14 提问）
UserChoice 子键除 ProgID 外还有 `Hash` 防伪印。机制：
- 手动设置（设置 UI）走**官方默认应用 API**，由系统用当前用户令牌生成合法 Hash + 广播 `SHChangeNotify` 让 Explorer 重载 + 触发缩略图缓存刷新 → 系统认可。
- 第三方脚本/程序**裸写注册表值**改 ProgID 或在被抢 ProgID 下挂 ShellEx：① 写不出合法 Hash（算法系统私有+绑定登录令牌），系统判定 UserChoice 损坏/无效→忽略或保持原值；② 触发不了系统级缓存刷新。
- 故「路B 注入 UserChoice 指向的 ProgID」也绕不过 Hash：只要 UserChoice 仍合法指向别人，往那条链挂 ShellEx 属寄生且不优雅，WPS 还可能注册自己的预览处理器挡在前面。
- **正确代码方向**：让程序调用官方 `IApplicationAssociationRegistration`/`SetAppAsDefault` 设自身为默认（同手动的官方通道，合法带 Hash）；或设置界面加按钮直接弹系统"默认应用"面板让用户点。换电脑运行一次即自动合法接管，portable。

## 实施：代码走官方API接管默认应用（02:18 起，用户批准"按你说的走官方通道然后备份"）
- 改动 `QuickView/SettingsOverlay.cpp`：
  1. 加 `#include <shobjidl.h>` + `#pragma comment(lib, "uuid.lib")`。
  2. 新增 `static bool SetQuickViewAsDefaultForExt(ext)`：封装 `IApplicationAssociationRegistration::SetAppAsDefault(L"QuickView", ext, AT_FILEEXTENSION)`（与 Windows 设置同源的官方通道，写合法 UserChoice Hash）；调用名 "QuickView" 对应 `RegisteredApplications` 注册名。
  3. `RegisterAssociations` 末尾(8b)对每个**用户选中的矢量格式**调上述封装，自动夺回默认应用（WPS/Edge 无法再劫持）。
  4. 修正 `Capabilities\FileAssociations` 映射：矢量格式→`QuickView.Vector`（带缩略图 provider 的 ProgID），光栅→`QuickView.Image`。
- 预期：程序运行/用户点关联即合法写 UserChoice 接管 `.svg` 等，缩略图稳定走我们 provider，portable（换电脑运行一次生效）。
- 官方API改动已编译通过（02:24:21，BUILD_EXIT=0），用户早前"出图了"已确认生效。

## 新根因：桌面 SVG 带 UTF-8 BOM → magic 检测失败（02:25 用户报"有的SVG能显、桌面几个不行"）
- 实测桌面 3 个 SVG 前 8 字节均为 `EF BB BF 3C 73 76 67 20` = UTF-8 BOM + `<svg `。
- `ThumbnailProvider.cpp` magic 检测（约 L353-355）只在缓冲区**第 0 字节**比 `<?xml`/`<?svg`/`svg`，带 BOM 时第 0 字节是 0xEF 不匹配 → 落到 L357-364 的 ASCII 兜底：BOM 字节无 NUL → `asc=true` → 误判 `ext=plt`(HPGL) → worker 按绘图文件渲染 SVG 失败 → 不出图。
- 能正常显示的 SVG 是无 BOM、第 0 字节即 `<svg` 的文件。App 本体打开不受影响（走真实扩展名+自有解析器），仅 Explorer 缩略图出此问题。
- 修复方向：magic 检测前跳过 UTF-8 BOM（EF BB BF），用跳过后的指针/长度做后续所有格式匹配。仅改 `ThumbnailProvider.cpp` 一处。
- 注：日志 `C:\Windows\Temp\qvthumb_provider.log` 被 provider 宿主以非共享句柄常开，本环境 PowerShell/Read 均无法稳定读取；根因已由源码+文件字节独立坐实，无需日志佐证。

## 编译部署坑：OneCommander 锁住 ThumbnailProvider.dll（02:53 实施）
- 重编译 `ThumbnailProvider.dll` 连续 2 次 `lld-link: error: failed to write output ... permission denied`，DLL 时间戳卡在昨天 20:36。
- 排查：EXE 能写、DLL 写不进 → 必是某进程加载了该 DLL 模块。后台 PowerShell 枚举进程模块，锁主为 `OneCommander`(PID 32200)——第三方文件管理器，常驻后台并加载缩略图 provider DLL；杀 explorer/dllhost、移走桌面 SVG 均无效，因为它才是真锁主。
- 解决：编译前 `Stop-Process -Name "OneCommander"` 释放锁，重编译 BUILD_EXIT=0，DLL_TIME=2026-08-11 02:53。
- **后续注意**：本机编译/替换 `ThumbnailProvider.dll` 前，必须先退出 OneCommander（或任何会加载该 DLL 的文件管理器），否则链接失败。Explorer 并非唯一锁主。
- BOM 修复已编进 DLL，SVG 已移回桌面、Explorer 已重启，待用户目视确认桌面 SVG 出图。

## 致命坑：后台脚本手动 Start-Process explorer 会致桌面空白（02:58 修复）
- 编译脚本末尾用 `Start-Process "explorer.exe"` 重启 Explorer。但在后台 PowerShell 上下文里，手动起的 explorer 实例让系统认为"explorer 仍存活"，Winlogon 不再自动重启真正的桌面 shell → 桌面（图标/任务栏/文件夹视图）全部空白，用户误以为缩略图坏了。
- 正确做法：重启 Explorer 时**只杀（`Stop-Process -Name explorer -Force`），不手动 Start-Process**；杀光后 Winlogon 会自动拉起桌面 shell（单一实例、Session=1、Title 空为正常）。若极端情况未自启，用任务管理器"运行新任务 explorer.exe"让用户交互启动。
- 教训：后台/非交互会话启动的 explorer 不一定是桌面 shell；不要既杀又手动起，避免抢占 Winlogon 的 AutoRestartShell。
- 02:58 已用"杀光+等 Winlogon 自启"修复，explorer PID=17160 Session=1 单实例，桌面恢复。
