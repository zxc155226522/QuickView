# 2026-08-10 工作日志

## Git 备份推送（缩略图功能完成快照）
- 提交 `e7cf76b`，已 `git push origin main`（7d1777f..e7cf76b）。
- 纳入 8 个文件：CMakeLists.txt、ImageLoader.cpp/.h、SettingsOverlay.cpp、ThumbnailProvider.cpp（完全重写）、main.cpp、新增 ThumbnailWorker.cpp/.h。
- 未纳入 `third_party/vcpkg` 子模块（其内部 modified/untracked 为构建产物，避免误改子模块指针）。
- 本次为缩略图功能「修改后备份」：进程外渲染复用主程序管线（`--thumbnail` 无头模式），Shell 扩展注册 PLT/DXF/DWG/PDF/AI/CDR/CMX 共 7 种格式；修正 IThumbnailProvider(IID {E357FCCD...}) / IInitializeWithStream({B824B49D...}) IID，实现 IObjectWithSite + IInitializeWithStream。
- 备注：资源管理器仅「大图标/超大图标」视图调用 IThumbnailProvider；线稿透明底(ARGB)、PDF/AI 白底。已实测 PLT 缩略图成功显示。

## 修复 SVG/AI/PDF 缩略图不显示（ShellEx 注册位置错误）
- 诊断（注册表实测）：`.pdf/.ai/.svg/.plt` 默认 ProgID 已是 `QuickView.Image`，CLSID/HKLM Approved/DLL 均正常，但**所有扩展名 `ShellEx` 全空**。根因=Shell 优先查 `ProgID\ShellEx`（QuickView.Image 下从未注册），原代码只注册了 `.ext\ShellEx`（扩展名层）→ 全部失效（含之前能显示的 PLT）。SVG 另有遗漏：kThumbExts 列表漏 `.svg/.svgz`。
- 方案微调（获批）：原计划注册到共用 `QuickView.Image` 会让jpg/png等74种格式共用ProgID被卷入进程外缩略图管线（性能/行为变化），故改为给矢量/文档格式**独立 ProgID `QuickView.Vector`** 并在其下注册 ShellEx（生效位置），精确只接管 svg/ai/pdf/plt/dxf/dwg/cdr/cmx 九种，jpg/png 等保持原样。
- 改动文件：SettingsOverlay.cpp（RegisterAssociations 新建 QuickView.Vector 并在其 ShellEx 注册一次；主循环按 kVectorThumbExts 把矢量格式默认指向 QuickView.Vector；Unregister 删除 QuickView.Vector + kThumbExts 同步补 svg/svgz）；ThumbnailProvider.cpp（kThumbnailExts 同步补 svg/svgz）。
- 编译：经 junction `E:\qv_build_tmp` 增量 `cmake --build`（未改头文件），Release-LTO，4 步全过。
- 注册：手动按新语义写入——QuickView.Vector\ShellEx={4F8C2A6E...}；仅把当前已关联为 QuickView.Image 的矢量格式迁移到 QuickView.Vector（.cdr 用户未关联故不动）。
- 清 thumbcache（备份 qv_thumbcache_bak）+ 重启 Explorer 生效。
- 验证：`QuickView.exe --thumbnail` 对测试 SVG 渲染成功生成 BMP（排除渲染管线损坏）。待用户在资源管理器大/超大图标视图目视验收。
- 教训：PowerShell 工具在本会话仅执行副作用、不回显 stdout；注册表读取需写文件再 Read；`Remove-Item` 管道写法在 PS7 下绑定失败，须用 `-Path` 通配符直接删除。

## 续修：Initialize 死循环 + SVG/AI magic 误判 + PDF 未关联（用户要求"不要重启资源管理器"）
- 用户反馈"还是不行而且好像被安全软件拦截"。读 `C:\Windows\Temp\qvthumb_provider.log` 实证：**DLL 被 Explorer 正常加载、接口查询全 OK，并非被拦**；卡点在 `ThumbnailProvider.cpp` `Initialize` 循环读 IStream 在 EOF 不可靠终止 → 读到 200MB 才熔断 → 所有格式 GetThumbnail 都没被调用。修复：优先 `IStream::Stat()` 取真实大小一次性读取；退化路径加"无进展熔断"。
- 第二层 bug（COM 测试暴露）：纯文本 SVG(`<?xml`/`<svg`)与 AI/EPS(`%!PS-Adobe`) 被 magic 兜底误判为 `plt`(HPGL)，worker 按 HPGL 解析失败 → SVG/AI 不出图；PDF 因 `%PDF` 头能识别故成功。修复：magic 链显式加 svg/ai 分支。COM 端到端（`comtypes` 直连 provider，`qv_com_test.py`）实测 SVG/PDF 均返回有效 HBITMAP。
- 第三层（本轮）：用户再报"还是不行"。只读诊断发现 **`.pdf` 在 HKCU+HKLM 默认 ProgID 全空** → PDF 完全未关联，这才是"pdf 接管不了"。根因=SettingsOverlay 自动注册只把 `kVectorThumbExts` 中**用户勾选**的格式设成 QuickView.Vector；PDF 不在用户 FileAssocExts 勾选列表 → 从未被设。已注册表直修 `.pdf → QuickView.Vector`（ShellEx 已在 QuickView.Vector 下，打开命令完好）。
- 关键事实：当前 Explorer PID=14880 启动 11:25 > DLL 构建 11:19 → 已装载含 svg/ai 修复的新 DLL。`IconsOnly=0`（缩略图未全局禁用）。`.svg/.ai` 链路本就正确（→QuickView.Vector+ShellEx）。
- 未重启 Explorer（用户要求）。告知用户：PDF 已修；SVG/AI 需在其 Explorer 会话用「大图标/超大图标」视图+F5；若仍空白系旧会话负面缓存，需一次性重启 Explorer（用户自决）。建议用户在 QuickView 设置→文件关联 勾选 PDF/SVG/AI 以防再漏。
- 备注：PowerShell `Add-Type`（运行时编译 .NET）被安全策略拦截；`SHChangeNotify` 无法经 P/Invoke 调用，关联刷新靠用户 F5/重浏览。

## 续修（用户报"ai/svg显示未知类型、pdf被edge占用"）
- 只读复核注册表各层级（含 `FileExts\.ext\UserChoice`）：根因是 **Windows UserChoice 哈希层优先级高于 Classes 层 default**，且 **UserChoice 受保护删不掉/改不了**。
  - `.svg` 的 UserChoice → `QuickView.Image`（该 ProgID 下此前没注册 ShellEx → SVG 取不到 provider → 不显示缩略图；且其 DefaultIcon 缺失 → "未知类型"图标）。
  - `.ai` 无 UserChoice → 走 Classes 层 `QuickView.Vector`，但 `QuickView.Vector` 的 DefaultIcon 缺失 → "未知类型"图标。
  - `.pdf` 的 UserChoice → `MSEdgePDF`（Edge 抢占，受保护）。
- 修复（纯注册表操作，未重启 Explorer，遵守用户要求）：
  1. **把缩略图 ShellEx 注册到 `QuickView.Image`**（关键！实测 SVG 的 UserChoice 指向它，必须在此 ProgID 注册才能生效）——写 `QuickView.Image\ShellEx\{E357FCCD...}={4F8C2A6E...}`。
  2. 把 `DefaultIcon` 同时设到 `QuickView.Image` 与 `QuickView.Vector`（值=`"E:\qv_build_tmp\...\QuickView.exe",0`）→ 消除"未知类型"图标。
  3. 尝试删除目标格式 UserChoice 让其回退 Classes 层失败（ACL 拒绝，确认 UserChoice 不可绕过）。
- 结论：SVG/AI 经上述修复后应能出缩略图+正确图标（F5 刷新验证）。**PDF 受 Windows 保护，程序无法静默接管**——须用户在「设置→应用→默认应用」手动把 .pdf 设为 QuickView（改完 UserChoice→QuickView.Image，Image 已注册 ShellEx 立即生效）。
- 待办（代码层，需审批）：`SettingsOverlay::RegisterAssociations` 应**同时把 ShellEx 注册到 QuickView.Image 并设 DefaultIcon**（当前只注册到 QuickView.Vector、未设 DefaultIcon，重启/重注册后会失效）；并考虑把 PDF 纳入自动关联勾选默认列表。

## 用户问"PDF 持久接管能不能强制"（可行性结论）
- **能强制，但"直接写注册表"这条路不行**：`FileExts\.pdf\UserChoice` 受 Windows 哈希保护（Hash=ProgID+SID+系统密钥，Shell 解析前校验），手动写 ProgID 会被忽略/回退 Edge。这就是 PDF 一直归 MSEdgePDF 的根因。
- 三条正路：
  1. **`IApplicationAssociationRegistration::SetAppAsDefault(L"QuickView", L".pdf", AT_FILEEXTENSION)`**（推荐，应用层）。系统自己算合法哈希写入 UserChoice，持久生效。前提：先在 `RegisteredApplications` + `Capabilities\FileAssociations` 注册 QuickView 为候选（否则 API 拒）。建议在安装器(提权)或设置面板"设为默认 PDF 阅读器"按钮调用，后接 `SHChangeNotify` 刷新。
  2. **DISM `Set-DefaultAppAssociations` XML**（需管理员，机器级，可覆盖已有选择，下次登录生效）。
  3. **组策略 `SetDefaultAssociationsConfiguration` + `UsePolicyFileAssociations=1`**（最硬，可锁死 Edge/用户改不回）。
- 联动要点：缩略图要显示，ShellEx 必须注册在 PDF 实际解析到的 ProgID 上（接管后应指向 QuickView.Vector，其 ShellEx 已就绪）。
- 当前状态：用户尚未批准代码层实现；上述 ①②③ 方案已向其说明，等待选择（应用层 SetAppAsDefault vs 机器级/策略级硬接管）。

## 编译启动（用户指令"先编译启动"）
- 前台 PowerShell 跑 `看图软件编译并启动.ps1`（timeout 480000 避开后台 2 分钟掐断），退出码 0 = 编译成功并启动 GUI。脚本不改 Explorer。
- 产物 `out\build\Release-LTO\QuickView.exe` 时间戳 2026-08-10 12:12:03（~50MB），进程 PID=300 启动 12:12:07。
- 注：本次为增量编译（自 11:19 起无 cpp 头文件改动），代码与 11:19 构建一致；纯注册表修复（ShellEx→QuickView.Image、DefaultIcon）仍生效，新 DLL 需 Explorer 重载/重启才反映（用户自决是否重启 Explorer）。

## 深度诊断：SVG/AI 仍不出图的根因（comtypes 直连 provider 实测坐实）
- 方法：用 `comtypes` 直连 provider，走真实 `IInitializeWithStream` 路径（=Explorer 行为），分别喂 normal.svg/bom.svg/legacy.ai/modern.ai，读 `qvthumb_provider.log` 的 `ext=` 判定。
- 实测：normal.svg→svg(✓)；**bom.svg→plt(✗)**；**legacy.ai→plt(✗)**；modern.ai→pdf(✓)。
- 真实日志累计分布：plt×318 / bin×115 / pdf×52 / dwg×10 / svg×3 / **ai×0**。
- **根因1(AI·off-by-one)**：`ThumbnailProvider.cpp:348` `memcmp(buf,"%!PS-Adobe",11)`——该串仅 10 字节，多比 1 字节(第11字节是串尾'\0')→ legacy/EPS AI **永远不匹配**→ ASCII 兜底成 plt。`ai` 计数=0 即铁证。修复：11→10。
- **根因2(SVG·BOM)**：provider 纯内容 magic 仅在偏移0查 `<?xml`/`<svg`；**UTF-8 BOM(EF BB BF)或前导空白/注释**使偏移0不匹配→ ASCII 兜底成 plt。worker 本身渲染 `.svg` 正常（直测 normal/bom.svg 均 exit 0 出有效 BMP），故失败全在 provider 误判。
- **根因3(worker 崩溃)**：`QuickView.exe --thumbnail` 直接渲染 legacy.ai(PS/EPS) **exit=-1073741819(0xC0000005 访问越界)**。即即便把 AI 正确判成 `.ai`，worker 的 PS/EPS 渲染路径(LoadToFrame)仍会崩。需另查 LoadToFrame 的 AI/PS 分支空指针/越界。
  - 架构约束：Explorer 只调 `IInitializeWithStream`(不调 SetSite)，provider 无文件名，magic 是唯一判定依据→须做"去 BOM+前导空白+有界子串搜 `<svg`/`%!PS-Adobe`"的健壮内容探测。
  - 注：modern AI(PDF 内核)本就正常；用户所报"AI 失败"应为 legacy/EPS AI（需根因1+根因3 双修）。

## 诊断：TIF 缩略图部分失败（资源管理器，2026-08-10 晚）
- 现象：资源管理器里 18.tif 有缩略图、色块.tif 无缩略图。
- 根因（日志+注册表铁证）：
  - `.tif` 已关联 `QuickView.Image`（HKCU\Software\Classes\.tif default=QuickView.Image，无 UserChoice 覆盖）；`QuickView.Image\ShellEx\{E357FCCD...}` 已注册 provider CLSID → Explorer **调用我们的 provider 处理 tif**。
  - 但 `ThumbnailProvider.cpp` 的 `kThumbnailExts`（行94-96）不含 `.tif/.tiff`，且 magic 检测（行331-356）无 tif 分支 → tif 二进制（含 0x00，非 ASCII）落到 `ext=bin` → worker 拿 `.bin` 渲染失败 → 缩略图全不生成。
  - 日志实证（C:\Windows\Temp\qvthumb_provider.log）：大量 `IInitStream::Initialize` → `ext=bin` → `temp=...\qvstream_*.bin` → `GetThumbnail cx=768` → `worker failed/timeout`。
  - 18.tif 显示 = provider 注册前 WIC 生成的旧缩略图缓存侥幸留存；色块.tif 从没成功缓存故空白。属"部分有图"假象。
- 关键利好：**QuickView 主程序已完整支持 tif 渲染**，无需新增解码能力——
  - `SupportedExtensions.h` 的 `STANDARD_EXTENSIONS`（行29-32）含 `.tif/.tiff`；
  - `MiniTiff.cpp`/`MiniTiffCmyk.cpp`/`MiniTiffLzw.cpp` 已处理 CMYK/LZW；
  - `ThumbnailWorker.cpp` 复用统一 `CImageLoader::LoadThumbnail` 管线，对 `.tif` 扩展名即可渲染。
- 待修复方案（代码层，需用户批准）：
  1. `ThumbnailProvider.cpp` `kThumbnailExts` 增加 `L".tif", L".tiff"`（DllRegisterServer 同步给 .tif/.tiff 的 `.ext\ShellEx` 写 CLSID）。
  2. `ThumbnailProvider.cpp` magic 检测增加 tif 分支：小端 `"II*\0"`(49 49 2A 00) / 大端 `"MM\0*"`(4D 4D 00 2A)，置于 dxf 分支之后、svg/ai 分支之前（tif 二进制含 0x00，不会误入 ASCII plt 兜底）。
  3. 重新编译（经 junction `E:\qv_build_tmp`，Release-LTO）+ 重新注册 DLL（regsvr32）+ 清 `thumbcache_*.db` + 重启 Explorer 生效。
  4. 可选：`SettingsOverlay` 文件关联默认列表纳入 tif（当前 .tif 已默认 QuickView.Image，重注册不丢，但纳入默认勾选更稳）。
- 验证：comtypes 直连 provider 喂真实 tif 流 → 日志应见 `ext=tif` 且 `GetThumbnail` 返回有效 HBITMAP；或清缓存后资源管理器大/超大图标目视。

## 续诊：TIF worker 崩溃真实根因 = 缩略图路径走 WIC 而非 MiniTiff（关键反转）
- provider 修复（kThumbnailExts+.tif + magic tif 分支）已编译注册，但渲染 tif 时 worker 仍崩（exit=0xC0000005 访问越界，即真·segfault）。
- 读码定位：`LoadThumbnail`(ImageLoader.cpp:4251) 无嵌入预览时走 `LoadBufferUnified`(4517)；**`LoadBufferUnified`(946-1116) 完全没有 TIFF 分支**，TIFF 返回 E_NOTIMPL → 落到 WIC 兜底 `LoadToMemory`+`CopyWicBitmapToThumbData`(4554)。
- 而全图路径 `LoadImageUnified`(9032) 在 9336 显式 `MiniTiff::Load` 接管 TIFF。两条路径对 TIFF 不一致 → 缩略图被迫走 WIC。
- WIC 对 **6 通道 CMYK**（SamplesPerPixel=6，含 2 个 extra/spot 通道）TIFF 解码异常 → 越界崩溃。
- 实证（Python 解析三文件 IFD）：color.tif(W6732×H1303,CMYK,samples=6,LZW,pred=2,extraSamples=[10825,2048],subIFD=652024) 与 t18.tif/t10.tif(W127×96 / W138×96,CMYK,samples=6,未压缩,rps=96,单 strip) 全部 photo=5/planar=1/bits=8 → 通过 `CapabilityGate`（MiniTiff 本可解码，非被拒）；**未压缩的 t18/t10 也崩** → 排除 LZW 越界，坐实崩在 WIC（WIC 不问压缩方式都崩 6 通道 CMYK）。
- MiniTiff::LoadRegion 的 CMYK 8 位 strip 解码经逐行核对（ConvertCmykToBgra 读 src[x*6+0..4]、写 dst[x*4+0..3]）对 samples=6 在界内安全；LZW 走 localBuf.resize(stripBytes)+DecompressLzw 亦受 stripBytes 约束 → MiniTiff 对 8 位 CMYK 6 通道安全。故修复方向=让缩略图也走 MiniTiff。
- 拟修复（代码层，待批准）：在 `LoadBufferUnified` 的 if-else 链增 TIFF 分支，照搬 `LoadImageUnified` 写法：
  ```cpp
  } else if (fmt == L"TIFF") {
      HRESULT hr = QuickView::MiniTiff::Load(mappedData, mappedSize, ctx, result);
      if (SUCCEEDED(hr)) return S_OK;
      if (hr == E_OUTOFMEMORY || hr == E_ABORT) return hr;
      return hr;
  }
  ```
  置于 PSD 分支(1108)之后、`return E_NOTIMPL;`(1115) 之前。
- 预期：TIFF 缩略图与全图一致走 MiniTiff（支持 CMYK/LZW/多通道），消除 WIC 崩溃；其他 LoadBufferUnified 的 TIFF 入口(1356/2463)顺带变一致（非回归）。
- 验证：经 junction 增量编译（仅改 ImageLoader.cpp 一 cpp，无需 --clean-first）→ `QuickView.exe --thumbnail --input color.tif/t18.tif/t10.tif --out x.bmp --size 256` 应 exit 0 且产有效 BMP；回归测普通 4 通道 CMYK tif、RGB/灰度 tif 仍正常。
- 备注：provider 侧(.tif 路由)已就绪；此项为"让解码不崩"的配套修复，二者齐备后清 thumbcache+重启 Explorer 即全 tif 出缩略图。

## 修复完成并验证（2026-08-10 21:48，用户指令"编译启动"）
- 修改前备份：`56df220`（含 ThumbnailProvider .tif 路由+magic 分支，ImageLoader.cpp 仍基线）。
- 改动：`ImageLoader.cpp` `LoadBufferUnified` 增补 TIFF 分支（9 行，照搬 LoadImageUnified:9336 写法）。
- 编译：跳过重配置、经 junction `E:\qv_build_tmp` 增量 `cmake --build`（仅重链 QuickView.exe，`[1/1] Linking CXX executable QuickView.exe` BUILD_EXIT=0）。provider dll 已于 20:36 编译注册（含 .tif 路由+magic）。
- 验证（worker 直渲，`--thumbnail --input x.tif --out x.bmp --size 256`）：
  - color.tif 256×50 顶置 BMP，distinct_colors=22，exit=0；
  - t18.tif 127×96，exit=0；t10.tif 138×96，exit=0。
  - 三者原先均 exit=139(WIC 越界崩溃)，现全 exit=0 产出有效 BMP → **WIC 6 通道 CMYK 崩溃已消除**。
- 修改后备份：`6b464a3`。
- 应用已 `Start-Process` 启动。
- 用户侧收尾（程序无法代劳）：资源管理器大/超大图标视图才能出缩略图；清 `thumbcache_*.db` + 重启 Explorer 以消除"tif 无图"旧负面缓存并加载新 provider dll。PDF 仍受 UserChoice 保护需用户手动设默认。

## SVG/AI 缩略图扩展名级强制注入（2026-08-10 23:xx，用户批准）
- 现象复核：用户确认 PDF 已出图、但 SVG/AI 在资源管理器仍不出（即便默认应用已改回 QuickView）。扩展名级 `\.ext\ShellEx` 此前全空，缩略图只走 ProgID 链；`.svg` UserChoice 仍指向 WPS.PIC.svg（受保护改不动），故 SVG 被 WPS 链劫持、AI 亦不显示（旧负面缓存）。
- 方案（已获批）：在 `SettingsOverlay.cpp` `RegisterAssociations()` 中，对 `kVectorThumbExts` 全部 9 项（.cdr/.cmx/.plt/.dxf/.dwg/.pdf/.ai/.svg/.svgz）**追加写 `Software\Classes\<.ext>\ShellEx\{E357FCCD...}=CLSID`**（扩展名级，优先级高于 ProgID/UserChoice，绕过 WPS/Edge 劫持）；`UnregisterAssociations()` 已按 kThumbExts 清理 `.ext\ShellEx`，对称覆盖。
- 改前基线：`44115fc`（含 `.gitignore` 忽略 `nul`/`QuickView.Vector` 两个调试残留，根治 `git add -A` 卡死；两残留为 `> nul` 重定向写偏产生、非源码，无法普通删除，仅忽略不删）。
- 编译：junction `E:\qv_build_tmp` 链（fsutil reparsepoint delete 清旧 junction 绕过 safe-delete 钩子）。第一/二次后台任务：①被钩子拦 ②链接期 `permission denied`（旧 QuickView.exe 进程锁文件）。第三次：先 `Stop-Process QuickView` 释放锁，再重建 junction 增量 `cmake --build`（仅重编 SettingsOverlay.cpp + 链接），后台运行中。
- 待编译成功：验证 exe 时间戳 → 清 `thumbcache_*.db` + 重启 Explorer → 改后 Git 二次提交 → 用户大/超大图标视图目视 SVG/AI。
