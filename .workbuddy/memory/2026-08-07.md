# 2026-08-07 工作日志

## 右侧分页预览面板（多页文档：CDR / PDF / AI）

- 完成分页预览面板全链路实现（docked 同容器，非浮层）：
  - 渲染：`UIRenderer::DrawPagePanel` —— 画布同色背景 + 1px 强调分隔线；缩略图懒加载（CDR 同步 `RasterizeSvgThumbnail`，PDF/AI 异步 `DocumentRenderController::RequestThumbnail` 双通道队列 → `ProcessPendingPageThumbs` 上传 GPU）。
  - 布局让位：`ComputeImageViewportLayout()` 减少 `layout.Right`，宽度写入全局 `g_pagePanelReservedWidth`。
  - 交互：缩略图点击跳页（PDF→`HandlePdfPageJump`、CDR→`HandleCdrPageStep`）；面板内滚轮 `ScrollPagePanel`；滚动条拖拽 `Begin/Drag/EndPagePanelScrollDrag`。
  - 配置：`ShowPagePanel`(bool) / `PagePanelWidth`(int 0=自动220/160~400) 写入 `General` 段 Save/LoadConfig。
- 修复跨 TU 链接错误（LNK2019）：`main.cpp` 中 `ResolveCanvasColor()` 与 `g_renderEngine` 去掉 `static`，改为外部链接（供 `UIRenderer.cpp` 的 `extern` 引用）。
- main.cpp 接入点：WM_LBUTTONDOWN(PageThumb/PagePanelScroll/PagePanelBackground)、WM_MOUSEMOVE/UP 拖拽、WM_MOUSEWHEEL 面板滚动、`ResultMessage` 缩略图结果处理、`g_pagedDoc.active=true` 与 `Reset()` 处调用 `ResetPagePanel()`。

## 编译验证

- 经 ASCII junction `E:\qv_build_tmp`（`New-Item -ItemType Junction`），preset `Release-LTO` 增量编译（仅改 .cpp/.h，未触发 vcpkg 重装）。结果见 build 日志 `E:\qv_build.log`。

## 回归排查：改 EditState.h 后"全部图空白"

- 现象：实现分页面板（46f78a4 干净提交）后，用户反馈普通图（jpg/png/webp）整片空白、UI 正常。
- 定位：加临时 `PDBG` 诊断（写 `E:\qv_dbg.log`）。干净全量构建（118 文件 `--clean-first`）加载合法红 BMP 表现完全正常：
  `FinalUp hr=0 bmp=1`（位图上传成功）、`RenderImgToDComp bmp=1 svg=0`（走位图路径烘焙）、`DrawStd` 绘制尺寸正确（200×200 / 600×600，非 0）。
- 关键结论：**空白是陈旧 6 文件增量构建的副作用**，不是功能代码缺陷。改动 `EditState.h`（AppConfig 增字段）后增量编译漏编依赖文件，运行期结构体布局/内联访问不一致，导致位图虽能上传（bmp=1）但绘制环节取到垃圾值 → 全图空白；单实例互斥 `Local\QuickView_Master_v1` 一直把旧实例留在前台，"启动"只是激活旧实例换图，看似"改了还空白"。
- 处置：做完干净全量重建（17:02 生成新 `QuickView.exe`）即恢复；随后移除全部 8 处 `PDBG` 调用 + 2 处宏定义（含 `#include <cstdarg>`），工作树回到与 46f78a4 完全一致（已 `git diff HEAD` 验证）。
- 教训：任何改动头文件（尤其被多 TU 包含的结构体）后，排查"回归"必须**先做一次 `--clean-first` 全量重建**再下结论；否则极易被陈旧增量构建误导。

## 临时诊断文件（已清理或留存于 E:\ 根）
- `E:\qv_dbg.log`、`E:\test_img.bmp`、`E:\qv_test\`、`E:\qv_build4.log`、`E:\qv_build5.log` 等为本次诊断产物，非项目文件。

## 用户二次反馈：回退到功能前 (18:23)

- 用户实跑 17:02 干净构建（46f78a4）后仍空白，要求 `git reset --hard a6dbb59`（分页预览功能开发前快照）。已执行：HEAD→a6dbb59，功能提交备份分支 `backup-pagepanel-46f78a4`，工作树干净。
- 重建：`--clean-first` 后台 PowerShell 两次均 2m1s 零输出 failed（**后台 2 分钟墙钟限制**掐断 LTO 链接，非代码错）。改**前台** PowerShell 增量链接（obj 已就绪）→ `BUILD_EXIT=0`，18:31 生成 a6dbb59 版 `QuickView.exe`。
- 诊断反转：此前"空白是陈旧增量构建、功能代码干净"结论不成立——用户实跑干净构建仍空白。根因未定：回退后实测若正常→功能代码确有牵连；若仍空白→根因在更早提交（6a34956 VectorLoader / GalleryOverlay 等），需 bisect。已请用户运行新版 EXE 验证普通图是否恢复。
- 清理：删除 `E:\qv_revert_build*.log` 临时构建日志。

## 去除缩放时屏幕下方百分比提示 (19:51)

- 需求：缩放时屏幕下方（`OSDPosition::Bottom`）弹出的「缩放: xx%」气泡不再显示。
- 改动：`main.cpp` 的 `ShowZoomOsd()`（约 3730 行）函数体改为直接 `return`，保留签名与全部调用点不变，不再调用 `g_osd.Show(...)`。
- 范围：仅去除实时缩放百分比（滚轮/按钮/快捷键/动画缩放）；适应屏幕/实际大小/填充窗口/复制/删除等其他 OSD 不受影响。
- 提交：`4cc8691`(备份) → `1e13e7b`(修改完成)，Release-LTO 增量编译通过。
- 恢复方式：若日后想恢复该提示，把 `ShowZoomOsd` 函数体改回原有的 `g_osd.Show(...)` 即可（或改造为配置开关）。

## 鸟瞰图(导航图) 默认定位右下角 + 背景复刻主画布 (20:05)

- 需求：鸟瞰图(总览图/导航图)定位右下角、显示尺寸与图同比例、背景与图一致、加边框。
- 核对现状：比例(`UIRenderer.cpp:5873-5883` 按图像宽高比算框体)与边框(`DrawRoundedRectangle` 白边+深色阴影)已满足；仅"右下角定位"与"背景与图一致"需改。
- 改动：
  - `EditState.h` `NavigatorAlignY` 默认 `0`→`1`(Bottom)；`main.cpp:4617` ini 读取默认值 `0`→`1`（与结构体默认一致，无 ini 即右下角，配合已有 `NavigatorAlignX=1`=右）。
  - `UIRenderer.h` 新增私有方法 `CreateCanvasBackgroundBrush(dc, uiScale)`；`UIRenderer.cpp` 在 `DrawNavigator` 中用其替换原深色半透明背景 `ColorF(0.05,0.05,0.05,0.75)`。该方法复刻 `main.cpp` 画布逻辑（CanvasColor==5 棋盘格预设/自定义棋盘格→2×2 棋盘格位图画刷；其他→纯色），使鸟瞰图背景与主画布完全一致（透明图棋盘格透出一致）。
- 编译：因改 `EditState.h`+`UIRenderer.h` 头文件，按血泪教训做 `--clean-first` 全量重建（junction `E:\qv_build_tmp`，preset `Release-LTO`），118/118 通过，生成 `QuickView.exe`。
- 提交：`037734c`(修改前备份) → `fa0aef2`(修改完成，备注含真实时间戳 2026-08-07 20:05:16)。
- 注意：已有 ini 且存过 `NavigatorAlignY=0` 的用户不会自动变右下角（保留其偏好），需拖拽或清配置；功能本就支持拖拽与设置里调整对齐。

## 鸟瞰图(导航图) 拖拽边角缩放 + 移动实时写入 (20:22)

- 需求（接续上条）：拖拽边角调整鸟瞰图大小、移动位置，停止操作 500ms 后实时写入 ini（防抖）。
- 改动（4 文件）：
  - `EditState.h` 新增 `float NavigatorScale = 1.0f;`（缩放倍率，比例保持图像宽高比；框体最长边 = `150*s*NavigatorScale`）。
  - `AppContext.h` `MinimapState` 新增 `bool isResizing`、`D2D1_RECT_F resizeGripRect`，`ResetLayout()` 补 `isResizing=false`。
  - `main.cpp`：
    - 读取：`NavigatorScale` 用 `GetPrivateProfileStringW`+`(float)_wtof`（同 OffsetX/Y 模式，默认 L"1.0"）。
    - 写入：`WriteConfigFloat(L"View", L"NavigatorScale", ...)`。
    - `HitTestMinimaps`：`MinimapHitResult` 加 `isResize`；inner 命中时先判 `resizeGripRect`（优先级高于拖动视口、不抢占关闭按钮）。
    - `WM_LBUTTONDOWN`：命中 `isResize` → `isResizing=true; SetCapture`。
    - `WM_MOUSEMOVE`：新增 `else if (minimap.isResizing)` 分支——以对齐角（固定锚点，缩放中坐标不变）为基准，按鼠标到锚点距离算长边 → `scale=clamp(longEdge/(150*s), 0.4, 4.0)`（≈60~600px），写 `NavigatorScale`，触发重绘 + 防抖保存。
    - `WM_LBUTTONUP`：条件并入 `isResizing`，松手 `SaveConfig()` 兜底并清状态。
    - 防抖：新增常量 `IDT_NAVIGATOR_SAVE=1002`（不与 `IDT_INTERACTION=1001` 冲突）；交互中 `KillTimer+SetTimer(...,500)`；`WM_TIMER` 加 `case IDT_NAVIGATOR_SAVE` → `KillTimer+SaveConfig`。移动(`isDraggingWindow`)分支同样走该防抖（满足"移动也实时写入延迟500ms"）。
  - `UIRenderer.cpp` `DrawNavigator`：
    - `maxDim` 改为 `150.0f * s * g_config.NavigatorScale`。
    - 计算 `resizeGripRect` 于"对齐角对角"（自由角），若该角=右上（与关闭按钮冲突，`AlignX=0,AlignY=1`）则改放左下；尺寸 `12*s`，存入 `minimap.resizeGripRect`。
    - 绘制两道对角斜线（半透明白）作为 resize 手柄视觉提示。
- 交互设计要点：grip 放在"自由角"（对齐角对角），拖拽时锚点（对齐角）固定、框体向自由角伸缩 → 直观且比例保持；resize 不影响 `NavigatorAlignX/Y/OffsetX/Y`（位置不变）。
- 编译：改 `EditState.h`+`AppContext.h` 头文件 → `--clean-first` 全量重建（junction `E:\qv_build_tmp`，preset `Release-LTO`），118/118 通过，生成 `QuickView.exe`（20:22）。
- 提交：`9963b97`（修改完成，备注含真实时间戳 2026-08-07 20:22:36）。
- 待用户验证：拖角落手柄缩放、移动位置，停止 500ms 后关窗重启确认 `QuickView.ini [View] NavigatorScale/AlignX/Y/OffsetX/Y` 已持久化。
